# Kubernetes Client

This is based from (https://github.com/lachie83/k8s-kubectl)

Currently set to kubectl v1.21.14

We have added tanka and jsonnet-bundler as we use these tools to generate our k8s manifests in CD before deploying.
